# Secrets Configuration Template
# Copy this file to secrets.tfvars and fill in your actual credentials
# NEVER commit secrets.tfvars to Git!

# Cross-account Bedrock credentials
cross_account_access_key_id     = "YOUR_ACCESS_KEY_ID_HERE"
cross_account_secret_access_key = "YOUR_SECRET_ACCESS_KEY_HERE"

# Usage:
# terraform apply -var-file="terraform.tfvars" -var-file="secrets.tfvars"
